pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                // Checkout the source code from the repository
                checkout scm
            }
        }

        stage('Build') {
            steps {
                // Build the project using Gradle
                script {
                    sh './gradlew clean build'
                }
            }
        }

        stage('Test') {
            steps {
                // Run tests using Gradle
                script {
                    sh './gradlew test'
                }
            }
        }


        stage('Deploy') {
            steps {
                // Deploy the application if necessary
                script {
                    echo 'Deploying application...'
                    // Add deployment steps here
                }
            }
        }
    }
    }
}
