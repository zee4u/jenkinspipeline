pipeline {
    agent any

    tools {
        // Install the JDK and Gradle
        jdk 'OpenJDK 11'
        gradle 'Gradle 6.7'
    }

    environment {
        // Set environment variables if necessary
    }

    stages {
        stage('Checkout') {
            steps {
                // Checkout the source code from the repository
                checkout scm
            }
        }

        stage('Build') {
            steps {
                // Build the project using Gradle
                script {
                    sh './gradlew clean build'
                }
            }
        }

        stage('Test') {
            steps {
                // Run tests using Gradle
                script {
                    sh './gradlew test'
                }
            }
        }

        stage('Package') {
            steps {
                // Package the application (e.g., create a JAR file)
                script {
                    sh './gradlew assemble'
                }
            }
        }

        stage('Deploy') {
            steps {
                // Deploy the application if necessary
                script {
                    echo 'Deploying application...'
                    // Add deployment steps here
                }
            }
        }
    }

    post {
        always {
            // Perform actions that always need to happen after the pipeline (e.g., clean up)
            cleanWs()
        }
        success {
            // Actions to perform if the pipeline succeeds
            echo 'Pipeline succeeded!'
        }
        failure {
            // Actions to perform if the pipeline fails
            echo 'Pipeline failed!'
        }
    }
}